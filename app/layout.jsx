import "./globals.css";

export const metadata = {
  title: "Printer Reader v3 + Batch",
  description: "100% Data Extraction • Batch Processing • Maximum Speed",
};

export default function RootLayout({ children }) {
  return (
    <html lang="th">
      <body>{children}</body>
    </html>
  );
}
